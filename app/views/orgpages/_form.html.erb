<%= form_for(@orgpage) do |f| %>
  <% if @orgpage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@orgpage.errors.count, "error") %> prohibited this orgpage from being saved:</h2>

      <ul>
      <% @orgpage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
